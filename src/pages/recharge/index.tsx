import CommonWarp from "@/components/CommonWarp";


interface RechargeProps { }

const Recharge: React.FC<RechargeProps> = (props) => {
  return (
    <CommonWarp title='充值' withHeader>
    </CommonWarp>
  );
};

export default Recharge;
